version: '3'
services:
  app:
    ports:
    - "3000:80"
    volumes:
    - ./LocalSettings.php:/var/www/html/LocalSettings.php
  mysql:
    image: mysql:5.7
    volumes:
    - mysql_data:/var/lib/mysql
    env_file:
    - .env
volumes:
  mysql_data: